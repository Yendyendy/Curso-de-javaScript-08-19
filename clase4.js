var nombre = "Can", edad = 28
//hay palabras reservadas que no puedes utilizar como 'var' 'funcion'

// function imprimirEdad() {
//     console.log(`${nombre} tiene ${edad} años`)
// }

// imprimirEdad() 


function frase(n, e) {
    console.log(`${n} tiene ${e} años`)
}
frase(nombre, edad)